window.AUTOBUILD_CONFIG = {
  // Your GitHub username
  owner: 'YOUR-USERNAME',
  
  // Your repository name
  repo: 'autobuild-web',
  
  // Workflow file name (don't change unless you renamed the workflow)
  workflowFile: 'autobuild-v2.yml',
  
  // GitHub Personal Access Token (optional - users can input via UI)
  // For security, leave this empty and let users input their own token
  token: null
};
